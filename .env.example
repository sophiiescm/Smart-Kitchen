# Kopiert diese Datei zu .env und passt die Werte an
# NIEMALS .env ins Git einchecken!

# MySQL-Datenbank
MYSQL_ROOT_PASSWORD=geheimespasswort
MYSQL_DATABASE=myapp

# JWT Secret Key – mit folgendem Befehl generieren:
# openssl rand -hex 32
SECRET_KEY=bitte-durch-echten-schluessel-ersetzen
